import React, { Component } from 'react'
import { Text, StyleSheet, View, TextInput } from 'react-native'

const Input = props => {
    return (
        <TextInput style={{height:30,width: 100, borderBottomColor: 'grey', borderWidth:1, marginVertical:10 }} />
    )
}

export default Input;
